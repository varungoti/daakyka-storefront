/** Default seed admin email (override via ADMIN_SEED_EMAIL). Swap for client credentials at go-live. */
export const DEFAULT_ADMIN_SEED_EMAIL = "varungoti@gmail.com";

/** RBAC test viewer — Gmail +alias delivers to the same inbox as the admin email. */
export const DEFAULT_VIEWER_SEED_EMAIL = "varungoti+viewer@gmail.com";

export const DEFAULT_ADMIN_SEED_PASSWORD = "Daakyka@2026";
export const DEFAULT_VIEWER_SEED_PASSWORD = "Daakyka@Viewer2026";
