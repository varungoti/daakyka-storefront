# Deprecated — OutfitAnyone GPU service

This folder is **deferred**. GPU-based OutfitAnyone inference was too slow for real-time storefront UX.

**Active service:** `../ar-tryon/` — MediaPipe + OpenCV CPU try-on.

See `docs/OUTFIT_STUDIO.md`.
